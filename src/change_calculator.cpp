#include "change_calculator.h"

using namespace std;

bool make_change(const vector<int> &values,
                 const vector<int> &counts,
                 int target,
                 vector<int> &result)
{
    return false; // Заглушка
}
